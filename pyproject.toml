[project]
name = "youtube-stream-markers"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-api-python-client>=2.159.0",
    "google-auth>=2.37.0",
    "google-auth-oauthlib>=1.2.1",
    "requests>=2.32.3",
]

[dependency-groups]
dev = [
    "icecream>=2.2.0",
    "pyright>=1.1.392.post0",
    "pytest>=8.3.4",
    "pytest-cov>=6.0.0",
    "ruff>=0.9.2",
]

[tool.pyright]
exclude = [".venv"]
